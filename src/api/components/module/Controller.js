const { httpCode, strings } = require('../../../constants')
const { logger } = require('../../../util')

module.exports = class /* Controller */ {
  // <function>(req, res) {}
  // <function>(req, res) {}
}
